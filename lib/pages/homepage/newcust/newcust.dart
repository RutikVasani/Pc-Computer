import 'package:flutter/material.dart';

class NewCustPage extends StatelessWidget {
  const NewCustPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
