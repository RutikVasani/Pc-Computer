import 'package:flutter/material.dart';

class TotalCollectionPage extends StatelessWidget {
  const TotalCollectionPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
