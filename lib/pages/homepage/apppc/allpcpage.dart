import 'package:flutter/material.dart';

class AllPcPage extends StatelessWidget {
  const AllPcPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
